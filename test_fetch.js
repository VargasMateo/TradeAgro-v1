async function test() {
  const token = process.env.MKL_TOKEN || 'test';
  console.log("We can't easily fetch authenticated routes without the token. Let's just check the data length if possible.");
}
test();
